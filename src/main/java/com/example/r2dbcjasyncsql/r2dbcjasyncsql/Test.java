package com.example.r2dbcjasyncsql.r2dbcjasyncsql;

import javax.swing.text.DateFormatter;
import java.time.Duration;
import java.time.Instant;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.time.temporal.Temporal;
import java.time.temporal.TemporalAmount;
import java.time.temporal.TemporalUnit;
import java.util.List;
import java.util.Locale;

/**
 * Created with IntelliJ IDEA.
 *
 * @author wangguo
 * @Date: 2019/7/29
 * @Time: 9:44
 * @Description:
 */
public class Test {

/*
    public static void main(String[] args) throws InterruptedException {
        // 获取当前时间
        LocalDateTime localDateTime = LocalDateTime.now();
        DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
        System.out.println(dateTimeFormatter.format(localDateTime));


        LocalDate localDate = LocalDate.now();
        localDate.format(dateTimeFormatter);
        System.out.println("localDate: "+localDate);


        // 指定时间
        LocalDateTime localDateTime1 = LocalDateTime.of(2019, 06, 10, 10, 30,30);
        System.out.println(localDateTime1);
        // 解析时间
       // LocalDateTime localDateTime2 = LocalDateTime.parse(localDateTime, DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
        //System.out.println(localDateTime2.getNano());

    }*/
}